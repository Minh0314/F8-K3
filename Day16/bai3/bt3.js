// Input:

// Cho trước 3 số a, b, c

// Output:

// Tìm số lớn nhất trong 3 số và hiển thị kết quả


var a =5, b=20, c = 15;
var max =a; 

if( b >a ){ 
   max = b 
}
if( c >b ){ 
    max = c
 }
   
console.log(` số lớn nhất là ` + max);